开发流程：

阶段1：你是资深C++架构师/存储引擎专家，现在从零开始在WSL环境下开发工业级LSM-Tree KV存储引擎（类似RocksDB/LevelDB），在 LSM-Tree 该目录下直接生成源码文件和工程文件就行

要求：

- 语言：C++17及以上

- 环境：WSL2 Ubuntu，GCC/Clang，CMake + Ninja

- 规范：Google C++ Style，线程安全，异常安全，无内存泄漏

- 输出：完整可编译项目结构、CMakeLists.txt、基础头文件、目录说明

第一阶段：请先生成：

1. 完整项目目录结构 工业级 LSM-Tree KV 存储引擎（C++）设计与实现.md ，这是项目架构参考文档

2. 根目录CMakeLists.txt（支持WSL，设置编译选项、Sanitizer、构建生成compile_commands,json文件）

3. 基础通用模块：

- Status.h/cc（错误码、状态）

- Slice.h/cc（轻量字符串引用）

- Arena.h/cc（内存池，减少malloc）

- Util.h/cc（CRC32、编码、哈希）

4. .gitignore

等我第二阶段指示

规范：

1、源码放在src目录，头文件和源文件放一起，不用include文件夹

2、如果需要第三方依赖使用vcpkg进行管理 然后cmake记得集成

3、主要注意工程功能实现不用生成测试代码还有说明文档啥的

4、必要的代码注释

5、模块化，结构清晰



阶段2：
实现LSM写入路径全链路（WAL → MemTable → Immutable）： 1. WAL（Write-Ahead Log） - 顺序追加、块式、CRC32校验 - 日志滚动、清理、崩溃重放 - 接口：Append/Recover/Close 2. MemTable（基于SkipList） - Put/Delete（逻辑删除tombstone） - Get/Scan/Iterator - 容量控制、线程安全 3. Immutable MemTable - 只读、不可修改 - 支持Flush到SSTable 4. 批量写 WriteBatch - 原子批量操作 - 支持Put/Delete

输出：完整实现，规范：适当注释

阶段3：
实现工业级SSTable（Sorted String Table）磁盘格式：

SSTable结构：

- Data Block（数据，前缀压缩、LZ4/ZSTD压缩）

- Filter Block（BloomFilter）

- Meta Index Block

- Index Block（key → block offset）

- Footer（固定大小，校验和）

要求：

- 块压缩：LZ4（默认）、ZSTD可选

- 二分查找、懒加载、mmap支持

- 线程安全读取、校验和

- 接口：Builder/Reader/Iterator/BloomCheck

输出：

- 代码：Block.h/cc、Table.h/cc、TableBuilder.h/cc

规范：适当注释，完整实现


阶段4：
实现LSM多版本控制（MVCC）与元数据持久化：

1. FileMetaData（SSTable文件元信息）

2. VersionEdit（层变更日志）

3. Version（某一时刻全量文件视图）

4. VersionSet（管理所有Version、Manifest）

5. Manifest（持久化版本变更，崩溃恢复）

要求：

- 原子提交、读写分离、Compaction不阻塞读

- 崩溃一致性

输出与规范：

- 完整代码实现

- 适当注释


阶段5：
实现LSM最核心：Leveled Compaction（RocksDB标准）

分层：
- L0：文件无序、可重叠
- L1~LN：层内key不重叠、大小逐层翻倍

功能：
1. Minor Compaction（MemTable → L0）
2. Major Compaction（上层→下层合并）
3. 多路归并k-way merge
4. 墓碑清理、旧版本删除
5. Compaction调度、IO限速、写抖动控制
6. 原子提交、废弃文件清理

输出及规范：
- 代码：Compaction.h/cc、MergeIterator.h/cc
- 完整代码实现
- 适当注释

